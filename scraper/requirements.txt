# TenderRadar — Python dependencies
# Pinned for reproducibility; update versions deliberately.

beautifulsoup4==4.12.3
requests==2.32.3
anthropic>=0.50.0
python-dotenv==1.0.1
lxml==5.2.2
httpx>=0.27.0
